class Menu {
  title: string
  image: string
  description: string
  id: number

  constructor(title: string, image: string, description: string, id: number) {
    this.title = title
    this.image = image
    this.description = description
    this.id = id
  }
}

export default Menu
